'use client';

import RequestLeave from "@src/components/RequestLeave";
// import { useFetchData } from '@src/services/useFetchData';
// import { getLogsPerDay } from '@src/utils/getLogsPerDay';
// import CardGraph from '@src/components/CardGraph';

// const Page = () => {
//   const { data: logs } = useFetchData<any>('/api/tasks/GET/getStatLogs');

//   if (!logs) return <p>Loading...</p>; // Show loading until logs are available

//   const filteredLogs = getLogsPerDay({ logs, userEmail: 'hannah19@gmail.com' });


//   return (
//     <CardGraph userChartData={filteredLogs}/>
//   );
// };

const Page = () => {
  return(
    <> 
      {/* <RequestLeave /> */}
    </>
  );
}
export default Page;
